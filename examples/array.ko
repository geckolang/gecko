extern func printf(template: Str, ...) -> Int

func main(argc: Int, argv: *Str):
  let first_4_primes = [2, 3, 5, 7]

  for i in 0..<4:
    unsafe: printf("%d\n", first_4_primes[i.as<U32>])

  return 0
