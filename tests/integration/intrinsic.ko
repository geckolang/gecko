struct Complex:
  first: Str,
  second: Int,

func size_of():
  let int_size = @sizeof(Int)
  let person_size = @sizeof(Complex)

-- TODO: No longer using the old `infer_type`.
-- func length_of():
--   let arr = [1,2,3]
--   let arr_len = @length_of(arr)
