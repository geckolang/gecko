# BUG: Why does it work when we remove the other function?
func inferReturnType(a):
  !a

  return !a

func test(a):
  let array = [1]
  let empty_array: [Int, 0] = []
  let b = if true: true else: false
  let c = !true

  return a and b and c
